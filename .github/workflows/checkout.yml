name: My Workflow
on: push
jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
      - run: ls -la
      - uses: actions/checkout@v4
        with:
          fetch-depth: 3
      - run: git log --pretty=oneline
