name: My Workflow
on: push
jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
      - run: ls -la
      - uses: actions/checkout@v3
        with:
          path: my-source
      - run: ls -la
      - run: ls -la my-source
